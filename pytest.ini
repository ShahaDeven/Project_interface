[pytest]
testpaths = tests
markers =
    slow: burns real wall clock (chaos=slow injects an 8s page delay)
    browser: drives a real Chromium against a real socket (needs `playwright install chromium`)
